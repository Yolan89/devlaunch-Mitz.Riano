/*
class Persona {
    name: string;

    constructor(name: string) {
        this.name = name
    }
    saludar = () => {
        console.log(`Hello, ${this.name} `);

    }


}

const persona = new Persona('Mitz')
persona.saludar()

*/

class Calculadora {

    Suma = (numeroA: number, numeroB: number, numeroC:number) =>{
        numeroA + numeroB; numeroC
     
    }
}

const Suma = new Suma(8,10);
Calculadora.Suma()